#!/usr/bin/env bash
# Install or refresh the secret-audit user systemd service + timer.
set -euo pipefail

script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
readonly script_dir

GITHUB_REPO_LINT_SERVICE_UNIT=secret-audit exec "${script_dir}/setup-service" "$@"
