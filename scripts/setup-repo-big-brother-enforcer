#!/usr/bin/env bash
# setup-repo-big-brother-enforcer — deprecated wrapper for setup-github-repo-lint.
set -euo pipefail

script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
readonly script_dir

printf 'WARN: scripts/setup-repo-big-brother-enforcer is renamed to scripts/setup-github-repo-lint\n' >&2
exec "${script_dir}/setup-github-repo-lint" "$@"

