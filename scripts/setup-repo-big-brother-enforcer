#!/usr/bin/env bash
# Install or refresh the repo-big-brother-enforcer user systemd service.
set -euo pipefail

script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
readonly script_dir

REPO_BIG_BROTHER_ENFORCER_SERVICE_UNIT=repo-big-brother-enforcer exec "${script_dir}/setup-service" "$@"
