#!/usr/bin/env bash
# Install or refresh the github-repo-lint user systemd service.
set -euo pipefail

script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
readonly script_dir

GITHUB_REPO_LINT_SERVICE_UNIT=github-repo-lint exec "${script_dir}/setup-service" "$@"
