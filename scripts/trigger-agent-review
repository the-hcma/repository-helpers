#!/usr/bin/env bash
# trigger-agent-review — request a review from the configured agent profile.
set -euo pipefail

script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
readonly script_dir

exec "${script_dir}/wait-for-agent-review" request "$@"
